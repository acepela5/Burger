<h1>Burgers Burgers Burgers!</h1>

<h2>Burgers You Can Order</h2>

<ul>
    {{#each burgers}}
      {{#unless devoured}}
        {{> burgers/burger-block devoured=true}}
      {{/unless}}  
    {{/each}}
</ul>

<h2>Burgers You Have Devoured</h2>

<ul>
    {{#each burgers}}
      {{#if devoured}}
        {{> burgers/burger-block devoured=false}}
      {{/if}}
    {{/each}}    
</ul>



<h2>Add A Burger</h2>

<form class="create-form">
    <div class="form-group">
        <label for="burgerNameInput">Burger Name</label>
        <input type="text" id="burgerNameInput" name="name">
    </div>

    <div class="form-group">
        <label for="devoured">DEVOURED!</label>
        <input type="radio" name="devoured" value="1">All Gone!
        <input type="radio" name="devoured" value="0">Yet To Be Eaten
    </div>

    <button type="submit">Add A Burger</button>
</form>